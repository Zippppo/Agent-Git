# Agent-Git 产品定义

Last updated: 2026-06-10

## 一句话定位

Agent-Git 是一个面向个人探索型工作的每日状态图。它用 Git 式的 `checkpoint`、`HEAD` 和 `fork` 心智，记录每个任务“做过什么、当前在哪、下一步是什么”，帮助用户每天快速恢复工作上下文。

它不是传统 todo list，不是团队项目管理平台，也不是 agent 控制台。MVP 只验证一个核心假设：**一张每日任务图，能否比零散笔记、聊天记录和白板更快地帮助用户重新进入状态。**

## 产品承诺

用户每天打开 Agent-Git 时，应该能在 30 秒内回答：

- 今天有哪些未完成任务？
- 每个任务当前的最新判断是什么？
- 哪些路线已经完成、废弃或发现风险？
- 下一步应该从哪里继续？
- 为什么当前会走到这个状态？

Agent-Git 的核心价值不是“提醒用户做事”，而是“恢复用户对复杂工作的理解”。

## 为什么需要它

很多复杂工作不是线性推进的，尤其是研究、实验、开发、调试、数据处理和 agent 协作。用户常常同时面对：

- 多个任务并行推进。
- 每个任务中都有试错、废弃路线、中途发现和临时判断。
- 昨天的最新结论，今天很难快速恢复。
- 普通 todo 只记录下一步，不记录为什么走到这一步。
- 聊天记录、实验日志、临时笔记和任务列表分散，恢复上下文成本高。

Agent-Git 把任务结果、推进过程和当前判断放在同一张每日图里，让用户不必重新翻材料来重建脑内状态。

## 目标用户

MVP 优先服务个人用户，而不是团队协作场景。

典型用户包括：

- 同时推进多个实验的研究员。
- 同时维护多个功能、bug、脚本和数据任务的工程师。
- 使用多个 AI agent 或自动化任务的个人开发者。
- 经常在不同上下文之间切换，需要快速恢复状态的人。

这些用户不一定需要完整项目管理系统，但需要一个轻量、持续、可追溯的工作状态面板。

## 核心使用场景

### 早上恢复状态

用户打开今天的 Daily Page，看到昨天遗留的未完成任务、每个任务的 HEAD、下一步动作和关键历史，快速决定今天从哪里开始。

### 任务中途试错

用户在某个任务下不断追加 checkpoint，记录做过的实验、观察到的问题、阶段性判断和下一步计划。

### 发现分支路线

当某个 checkpoint 产生替代方案或新的思路时，用户通过 fork 保留分支，而不是把所有内容混在一条线里。

### 收尾和过日

任务完成后留在历史快照里。新一天只继承未完成任务，让今天的页面保持干净，同时不丢历史。

## 核心产品原则

### 恢复上下文优先

所有功能都应该服务于“更快恢复状态”。如果一个功能只增加记录量，但不能降低重新进入状态的成本，就不应该进入 MVP。

### 一屏看全局

默认展示当天所有未完成任务。任务根节点横向排列，优先级从左到右。用户不应该为了理解当前局面而进入多个列表、详情页或弹窗。

### 每天一个快照

每天是一个新的 Daily Page。未完成任务继承到新一天，完成任务留在历史快照里。这样既保留历史，又让今天的工作面保持清爽。

### 历史不删除

废弃方案、错误路径、中途发现都应该保留。它们不是噪音，而是解释当前 HEAD 的上下文证据。

### HEAD 是状态锚点

每个任务应该有一个明确的 HEAD，表示“这个任务当前走到哪里”。HEAD 不是完成状态，而是用户恢复上下文时最重要的定位点。

### 画布是主操作区

用户应该能直接在任务图上完成高频操作。右侧面板是详细编辑区，不应该成为唯一编辑入口。

### 先手动，后自动

MVP 应该先验证用户是否愿意维护这张状态图。agent 自动写入、日志总结、截图识别等能力可以后置，避免过早把产品做成自动化平台。

## 信息模型

MVP 只保留四个核心概念：

- `Day`: 每日快照页面。
- `Task Root`: 一个并行任务的根节点。
- `Checkpoint`: 某个任务中的一步进展、发现、试错或计划。
- `HEAD`: 某个任务当前最新状态。

当前数据关系：

```text
Day
└─ Task Root
   ├─ Checkpoint
   ├─ Checkpoint
   └─ HEAD
```

`Task Root` 的状态包括：

- `Active`: 正在推进。
- `Blocked`: 被外部条件或关键问题卡住。
- `Paused`: 暂停，但不代表失败。
- `Done`: 已完成，不再继承到新一天。

`Checkpoint` 的状态包括：

- `Planned`: 计划中的下一步。
- `Done`: 已完成的一步。
- `HEAD`: 当前最新判断或继续点。
- `Finding`: 中途发现、风险、问题或重要观察。
- `Abandoned`: 废弃路线、错误路径或不再继续的方案。

## MVP 范围

当前 MVP 应该做到：

- 显示一个每日任务图。
- 横向展示多个 Task Root。
- 每个 Task Root 内纵向展示 Checkpoint。
- 支持新增 Task Root。
- 支持在当前任务下按 `Enter` 新增 Checkpoint。
- 支持在当前 Checkpoint 上按 `Tab` fork 分支。
- 支持按 `Space` 在画布内直接编辑当前 Task Root 或 Checkpoint。
- 支持点击 Checkpoint 左侧圆点切换状态。
- 支持设置 HEAD，并保证同一任务内只有一个 HEAD。
- 支持完成 Task Root。
- 支持拖拽重排 Task Root 和 Checkpoint。
- 支持每日快照继承未完成任务。
- 支持本地持久化和 JSON 导出。

MVP 的成功标准不是功能数量，而是用户是否真的会把它作为每日恢复状态的入口。

## 暂时不做

为了保持 MVP 清晰，第一版暂不做：

- 多人协作。
- 云同步。
- 权限系统。
- 评论系统。
- 复杂标签、筛选、搜索。
- 项目级工作区。
- agent 执行控制台。
- 传统看板、甘特图或日历视图。
- 大型数据库后端。

如果后续需要扩展，也应该先验证这些功能是否服务于“恢复上下文”这个核心目标。

## 交互思路

### 键盘负责快速生长

Agent-Git 的编辑体验应该接近思维导图工具：

- `Enter`: 延续当前任务链路，新增下一个 Checkpoint。
- `Tab`: 从当前 Checkpoint fork 出一条分支。
- `Space`: 编辑当前节点内容。

这些操作应该发生在画布上，而不是依赖表单面板。

### 鼠标负责状态和空间

鼠标更适合处理视觉状态和布局：

- 点击 Task Root 或 Checkpoint 进行选择。
- 点击 Checkpoint 圆点切换状态。
- 拖拽 Task Root 调整优先级。
- 拖拽 Checkpoint 整理顺序。
- 展开或收起左右侧面板，为画布释放空间。

### 右侧面板负责精修

右侧 inspector 保留 Task Root / Checkpoint 的完整编辑能力，但高频编辑必须能直接在卡片中完成。用户收起右侧面板时，仍然应该能持续工作。

## 当前应用状态

当前主应用已从最初的单文件原型迁移为 Vite + Electron 桌面应用：

- Renderer 入口在 `src/main.ts`，样式在 `src/styles.css`。
- Electron 主进程在 `electron/main.mjs`，使用隔离 preload 暴露附件能力。
- 使用 `localStorage` 保存任务图和偏好，图片附件保存在 Electron `userData` 目录下。
- 左侧面板可收起成窄 rail。
- Checkpoint 支持内联编辑、状态浮层、附件、折叠已完成节点和简单 fork 缩进。
- Task Root 支持内联编辑、状态切换、复制粘贴、删除和拖拽重排。
- Daily Snapshot 支持继承未完成任务。

`docs/mvp-single-file.html` 保留为原始单文件原型参考，不再是主开发入口。

## 已知限制

当前原型为了验证速度，接受以下限制：

- 数据模型还没有跨日 lineage，继承任务时会复制任务和 checkpoint。
- fork 目前主要是视觉缩进，还不是完整分支树。
- 没有撤销、删除、归档和导入能力。
- 没有搜索和跨日任务演化视图。
- JSON 导出适合开发验证，不一定适合普通用户。
- 主 renderer 仍是迁移后的单个大文件，后续需要拆分状态、渲染和交互逻辑。

这些限制不影响 MVP 验证，但会影响下一阶段产品化。

## 后续迭代方向

### 1. 强化恢复状态体验

优先优化用户打开页面后的第一眼信息：

- 更突出 HEAD 和 Next。
- 降低旧 checkpoint 的视觉噪音。
- 让 Finding / Blocked 更容易被看到。
- 给每个任务提供更清晰的“从这里继续”入口。

### 2. 更好的任务继承规则

当前继承逻辑是：新一天继承未完成任务。后续可以进一步定义：

- 哪些状态会被继承。
- HEAD 是否自动放到最显眼位置。
- Abandoned checkpoint 是否默认折叠。
- Done checkpoint 是否在新一天保持可见。

### 3. 更强的历史回看

Daily Page 的历史价值很高。后续可以增加：

- 按日期回看。
- 对比昨天和今天的 HEAD。
- 查看某个 Task Root 的跨日演化。
- 理解某个判断是从哪一天、哪个 checkpoint 开始形成的。

### 4. 更自然的分支表现

当前 fork 只是视觉缩进。后续可以探索：

- 更清晰的分支线。
- 折叠/展开分支。
- 将某个 fork 提升为主线。
- 标记 fork 的原因和最终结论。

### 5. 更轻量的数据导入导出

JSON 导出适合开发验证，但不一定适合用户。后续可考虑：

- Markdown 导入导出。
- 从纸面日志或截图提取任务。
- 与本地文件同步。
- 把 Daily Page 导出成可读日报。

### 6. 与 agent 工作流连接

如果未来要与 agent 协作，Agent-Git 不应该变成 agent 控制台。更合理的方向是：

- agent 运行结束后自动建议 checkpoint。
- agent 输出摘要后建议更新 HEAD。
- 用户确认后再写入 Daily Page。
- agent 的失败、分支尝试和最终结论都能沉淀为任务历史。

## 下一轮设计重点

下一轮 UI 设计不应该追求“功能更多”，而应该验证三个体验问题：

1. 用户能否一眼看出每个任务当前停在哪里？
2. 用户能否毫不犹豫地追加下一步或 fork 分支？
3. 用户是否愿意把这张图作为每天工作的起点？

视觉上应该保持工作台气质：信息密度高、结构稳定、操作直接、状态清楚。不要把它设计成营销页、普通看板或聊天应用。

## 判断标准

后续每个功能都应该用下面的问题检查：

1. 它是否帮助用户更快恢复上下文？
2. 它是否让每日任务图更清晰，而不是更复杂？
3. 它是否能在画布上完成，而不是强迫用户进入表单？
4. 它是否保留了关键历史，而不是只留下最终结果？
5. 它是否仍然适合个人高频使用？

如果答案不明确，就先不要加入 MVP。
